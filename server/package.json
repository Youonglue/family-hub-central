{
  "name": "familyhub-server",
  "version": "1.0.0",
  "description": "Self-hosted Family Hub server (Fastify + SQLite + WebSocket)",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/static": "^7.0.4",
    "@fastify/websocket": "^10.0.1",
    "better-sqlite3": "^11.3.0",
    "fastify": "^4.28.1"
  }
}
